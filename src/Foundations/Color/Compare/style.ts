import { css } from 'emotion'

const styles = {
  table: css({
    borderCollapse: 'collapse',
    fontSize: 14,
  }),
  thead: css({
    th: {
      minWidth: 143,
      fontWeight: 400,
      textAlign: 'left',
    },
  }),
}

export default styles
